flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
requests>=2.31.0
apscheduler>=3.10.0

# Sprint 4 — Export PDF
# Installation : pip install weasyprint --break-system-packages
# Ubuntu/Debian : sudo apt-get install libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz0b
# macOS         : brew install pango
# Render        : ajouter dans render.yaml les apt packages (voir Sprint 4 plan)
weasyprint>=60.0

# Base de données (production)
# psycopg2-binary>=2.9.0

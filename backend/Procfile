# Procfile pour Render (Gunicorn)
# Worker gthread avec timeout 120s pour les requêtes LLM longues
web: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --worker-class gthread --threads 4 --timeout 120
